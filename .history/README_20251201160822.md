## 📌 I. Yêu Cầu Tiên Quyết

Trước khi bắt đầu, hãy đảm bảo bạn đã cài đặt:

⚙️ 1. Node.js & npm/Yarn

Khuyến nghị sử dụng Yarn để tối ưu tốc độ cài đặt và ổn định hơn.

Kiểm tra phiên bản:

node -v
yarn -v

📱 2. Android Studio

Cài đặt Android SDK, Platform Tools, Build Tools.

Tạo Android Emulator (AVD) nếu muốn chạy máy ảo.

🍎 3. Xcode (Chỉ dành cho iOS – macOS)

Cài đặt command-line tools:

xcode-select --install

## ▶️ II. Khởi Chạy Ứng Dụng

Ứng dụng React Native (Bare Workflow) cần phải build phần Native và khởi động Metro Bundler.

### 1. Khởi động Thiết bị
Android:

Mở Android Studio → Device Manager → Start AVD

Hoặc dùng lệnh:

emulator -avd Tên_Máy_Ảo

iOS:

Mở iOS Simulator qua Xcode

Hoặc lệnh:

npx react-native run-ios

### 2. Build & Run (Lần đầu hoặc Full Build)

Sử dụng các lệnh sau:

Android
Nền tảng	Lệnh
npm	npm run android
Yarn	yarn android
iOS
Nền tảng	Lệnh
npm	npm run ios
Yarn	yarn ios

👉 Lưu ý: Các lệnh trên đã bao gồm việc tự động khởi động Metro Bundler → Bạn không cần chạy npm start hay yarn start riêng.

### 3. Cài đặt Phụ Thuộc Native (iOS)

Chỉ cần khi:

Lần đầu setup dự án trên macOS

Clone dự án từ Git

Thay đổi dependencies native

Chạy Bundler:
bundle install

Cài đặt CocoaPods:
bundle exec pod install

## 🛠 III. Phát Triển & Gỡ Lỗi (Fast Refresh)
### 1. Chỉnh sửa mã nguồn

Mở file:

App.tsx


Khi lưu, ứng dụng sẽ tự động reload (Fast Refresh) và giữ nguyên state.

### 2. Chạy Metro Bundler (Trong lúc phát triển)

Nếu Metro Bundler bị tắt, chỉ cần chạy:

npm start


hoặc:

yarn start


👉 KHÔNG cần chạy lại npm run android khi chỉ sửa JavaScript.

### 3. Reload toàn bộ ứng dụng
Android:

Nhấn R hai lần

Hoặc mở Dev Menu: Ctrl + M → Reload

iOS:

Nhấn phím R trong iOS Simulator