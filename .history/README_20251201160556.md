🚀 Bắt Đầu Dự Án React NativeChào mừng bạn đến với dự án của chúng tôi! Dưới đây là các bước cần thiết để khởi chạy ứng dụng.I. Yêu Cầu Tiên QuyếtNode.js & npm/Yarn (khuyến nghị sử dụng Yarn).Android Studio (cài đặt SDK và tạo AVD/Máy ảo).Xcode (chỉ cho phát triển iOS).II. Khởi Chạy Ứng DụngQuá trình chạy ứng dụng Bare Workflow yêu cầu bạn phải xây dựng phần Native và khởi động Metro Bundler.1. Khởi động Thiết bịTrước tiên, hãy đảm bảo rằng máy ảo hoặc thiết bị vật lý đã sẵn sàng:Android: Khởi động một Android Emulator (AVD) qua Android Studio hoặc lệnh emulator -avd [Tên_máy_ảo].iOS: Khởi động một iOS Simulator qua Xcode hoặc lệnh npx react-native run-ios.2. Build và Run (Lần đầu hoặc Full Build)Sử dụng các lệnh sau để tự động khởi động Metro Bundler, biên dịch mã Native, và cài đặt ứng dụng (.apk/.app) lên thiết bị/máy ảo.Nền tảngSử dụng npmSử dụng YarnAndroidnpm run androidyarn androidiOSnpm run iosyarn iosLưu ý quan trọng: Lệnh này đã bao gồm cả việc khởi động Metro Bundler. Bạn không cần chạy npm start hay yarn start trong Terminal riêng cho lần chạy đầu tiên.3. Cài đặt Phụ thuộc Native (Chỉ dành cho iOS)Nếu bạn đang phát triển trên iOS hoặc lần đầu clone dự án, hãy đảm bảo các phụ thuộc CocoaPods đã được cài đặt:Chạy Ruby Bundler lần đầu (nếu cần):Bashbundle install
Cài đặt hoặc cập nhật CocoaPods dependencies:Bashbundle exec pod install
Xem thêm hướng dẫn chi tiết tại CocoaPods Getting Started guide.III. Phát Triển và Gỡ lỗi (Fast Refresh)Sau khi ứng dụng đã được cài đặt và chạy thành công trên thiết bị, bạn có thể thực hiện thay đổi code một cách nhanh chóng.1. Sửa đổi mã nguồnMở file App.tsx (hoặc các file code khác) và thực hiện thay đổi. Khi bạn lưu file, ứng dụng sẽ tự động cập nhật và hiển thị thay đổi nhờ cơ chế Fast Refresh (giữ lại trạng thái ứng dụng).2. Lệnh Development (Chỉ cần Metro)Nếu Metro Bundler bị tắt trong quá trình phát triển, bạn chỉ cần mở Terminal mới và chạy lệnh sau để tiếp tục làm việc, không cần phải chạy lại npm run android:Bash# Sử dụng npm
npm start

# HOẶC sử dụng Yarn
yarn start
3. Tải lại toàn bộ (Full Reload)Để buộc ứng dụng tải lại toàn bộ hoặc reset trạng thái, hãy sử dụng các phím tắt sau:Android: Nhấn R hai lần hoặc mở Dev Menu (<kbd>Ctrl</kbd> + <kbd>M</kbd>) và chọn "Reload".iOS: Nhấn <kbd>R</kbd> trong iOS Simulator.